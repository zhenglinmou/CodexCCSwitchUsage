# Release Notes Template

Replace the placeholders before publishing. `scripts/publish-release.ps1` always appends the required browser-companion download, installation, and SHA-256 section; do not duplicate it here.

## This Release

- Describe the user-visible changes.
- List any migration, configuration, or compatibility requirement.

## Verification

- `npm test`: `<passed>/<total>`
- Installer upgrade: `<result>`
